Paste screenshot of your window, drag select a region using "s", open gpt as overlay on your selected region using "c".
Zoom in and out of chat using "ctrl" + "+" or "ctrl" + "-"

Future dev - 
- Allow wrapper background customization
- Changing opacity of the wrapper